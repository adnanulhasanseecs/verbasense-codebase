# Backend (copy to backend/.env or export in shell)
# Default API port 8011 — pairs with frontend on 3011 (see scripts/manage.ps1)
PORT=8011
MOCK_DELAY=400
MOCK_FAILURE_RATE=0
CORS_ORIGINS=http://localhost:3011

# Frontend (copy to frontend/.env.local)
NEXT_PUBLIC_API_URL=http://localhost:8011
